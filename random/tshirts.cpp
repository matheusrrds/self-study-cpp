#include <iostream>
#include <vector>
#include <string>

using namespace std;

// ordenar alfabeticamente as cores

int main() {

    while (true) {
        int n;
        cin >> n;
        cin.ignore();

        if (n == 0) {
            break;
        }

        string name;
        string color;
        string size;

        getline(cin, name);
        cin >> color >> size;


    }



    return 0;
}